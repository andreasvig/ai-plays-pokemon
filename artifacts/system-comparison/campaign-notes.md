# Old vs new system comparison — running notes

> Source: conversation 2026-09-06. Plan: [plan.md](plan.md). Runner: `test_scripts/run_system_comparison.py`; summarizer: `test_scripts/summarize_system_comparison.py`. Campaign dir: `local/system-comparison/` (gitignored).

## Setup findings (before the campaign)

1. **GPT-6 Astra probes (openai tag, effort medium).** Replayed `reasoning_details` (encrypted + summary items) are tokenized by the endpoint: +57 prompt tokens per replayed turn, versus 0 for a bare `reasoning` string. Verdict "consumed", so the profile keeps `reasoning_replay: all`. Caching is automatic on the whole prefix: an 8.5k-token identical prefix came back 8489/8492 cached on both calls, with and without a system marker, at $1/M (90% off). Profile `cache_mode: implicit`, no markers. Files: `artifacts/provider-compatibility/reasoning-replay-probe__openai--gpt-6-astra.json`, `cache-hit-probe__openai--gpt-6-astra.json`. Probe spend ≈ $0.06.
2. **Registry entries** `gpt-6-astra`, `gemini-3.8-flash`, `glm-5.3-flash` added to `configs/models.yaml`, each pinned with `provider.only` + `allow_fallbacks: false` to the tag the append-agent profile uses. Release dates synced.
3. **Legacy transport vs Z.AI.** First smoke (old arm, GLM): HTTP 400 `Tool choice must be auto` on the forced tool call the pydantic-ai path uses. `output_mode: native_json` then failed client-side (pydantic-ai: "Native structured output is not supported by the model"), so the GLM registry entry uses `output_mode: prompted` (locally validated JSON, the Gemma/Qwen path). The new arm sends tools with `tool_choice: auto` per its profile, so the two arms use different output plumbing for GLM; both are schema-validated.
4. **mGBA Lua auto-load flake.** The second child in a campaign got "Load recent script" (AppleScript load missed) and timed out after 60 s. The child now re-issues the load and waits again, up to four times.
5. The per-run dashboard cannot bind :3420 while the review server holds it; the run continues without it (logged as ERROR, harmless).

## Campaign log (2026-09-06, started 14:59)

- **glm-5.3-flash — old**: 25/25 turns, 19.2 min, $0.026 all-in, no retries. Checkpoints: left bedroom turn 3, outside turn 6, Oak's Lab turn 9; no starter in 25 turns. Prompt size hovered 8–10k tokens (sliding window), occasional 15–19k spikes. Run `2026-09-06_14-59-34_config-old__glm-5-3-flash-high`.
- **glm-5.3-flash — new**: 25/25 turns, 14.9 min, $0.0215 all-in, no retries, one compaction after turn 15 ($0.0009, 29 s). Checkpoints: left bedroom, outside, Oak's Lab at turn 16 (old arm: turn 9); no starter either. Prompt grew 3.3k → 26.5k tokens across the run, 85% of gameplay prompt tokens served from cache. Versus old: mean wall 35.8 s vs 46.2 s per turn, mean LLM time 22.0 s vs 35.1 s, total cost −17%. Same furthest checkpoint, reached later. Run `2026-09-06_15-18-56_config-new__z-ai-glm-5-3-flash`.
- **gemini-3.8-flash — old**: 25/25 turns, 11.0 min, $0.393 all-in, no transport retries. Checkpoints: left bedroom 2, outside 7, Oak's Lab 11, starter chosen 16, first rival battle done 25 — the strongest run so far. Reasoning volume is spiky (turn 5: 13.3k reasoning tokens, $0.056; typical turns ~1k, $0.01). Run `2026-09-06_15-33-58_config-old__gemini-3-8-flash-medium`.
- **gemini-3.8-flash — new**: 25/25 turns, 11.2 min, $0.483 all-in (+23% vs old), one compaction after turn 15 ($0.031, 5 s), 1 transport error + 1 output retry absorbed. Checkpoints: bedroom 1, outside 3, Oak's Lab 8, starter 11, rival battle 20, **Route 1 at turn 25** — one rung further and every rung earlier than the old arm. Mean LLM time 7.9 s vs 10.8 s; wall clock equal (26.8 vs 26.5 s) because emulator settle dominates. Cost per turn rose from $0.010 (first 5) to $0.0215 (last 5) as the prompt grew 2.5k → 26.7k tokens with only 6.8% cached — the known Gemini limit (only the system block is cacheable through OpenRouter). Self-grade true-rate 0.83 vs 0.50. Run `2026-09-06_15-45-09_config-new__google-gemini-3-8-flash`.
- **gpt-6-astra — old (in flight)**: turns 2–5 report **0 reasoning tokens** at effort medium (probe at medium gave 12–55 on a spatial question), ~$0.056–0.064 per turn on a ~7k prompt, 4–7 s per LLM call. Verify the reasoning parameter reached the endpoint before reading the arm.
- **gpt-6-astra — old**: 25/25 turns, 9.2 min, $1.72 all-in, no retries. Checkpoints: bedroom 2, outside 4, Oak's Lab 7, starter 10, rival battle 18, Route 1 at 25 — ties Gemini-new for furthest. Reasoning tokens per turn: 0 on 22 of 25 turns (22/27/41 on the other three) at effort medium; the new arm's first two requests also report 0, so near-zero reasoning is how Astra treats these turns at medium, not a legacy-transport artifact. Billing: turn 1 = 2,912 prompt × $12.50/M + output, i.e. OpenRouter bills OpenAI **cache writes at 1.25×** on the first request (OpenAI direct does not charge cache writes). Run `2026-09-06_15-56-27_config-old__gpt-6-astra-medium`.
- **gpt-6-astra — new**: 25/25 turns, 9.7 min, **$4.61** all-in (2.7× the old arm), one compaction after turn 15 ($0.42, 28 s). Checkpoints: bedroom 2, outside 4, Oak's Lab 7, starter 9, rival battle 15, **Route 1 at turn 18** (old arm: 25). Mean LLM time 4.6 s vs 5.3 s; wall 23.3 vs 22.0 s. Only 7.8% of prompt tokens cached: every request cached exactly 1,192 tokens (system + first user text) and paid the 1.25× cache-write price on the rest; per-turn cost $0.07 → $0.20 by turn 25 as the prompt grew 2.9k → 20k. Run `2026-09-06_16-05-44_config-new__openai-gpt-6-astra`.
- **Why Astra's cache did not extend** — investigated with replays of the saved request bodies (details in `artifacts/provider-compatibility/openai-image-prefix-caching-probe.md`): identical repeats hit fully, the next turn's body hits only up to the first screenshot; removing images makes it extend; but a hand-built body with the same screenshots, tools and system prompt extends too. Each field of the real body was swapped in isolation (stream, session id, reasoning fields, refusal, require_parameters, user texts, tool args, call ids, first user message, system length, image bytes) — none flipped it. Mechanism unresolved; ~$2 of probes. Open task in the Marvin brain.

## Campaign finished 16:15. Results: [results.md](results.md) (generated), verdict: [findings.md](findings.md).
